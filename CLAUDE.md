# CLAUDE.MD – 强制行为规则

当本对话中的任何代码生成或修改时，你必须自动执行以下闭环：

1. 进行测试，展示完整输出。
2. 若全部通过 → 跳至第4步。
3. 若存在失败 → 分析错误、仅修复相关代码后重新测试（最多额外重试4次，总计不超过5次）。
4. 输出执行报告（包含修改概要、测试结果）。
    - 通过：写“任务完成”
    - 5次后仍失败：写“任务未完成”，附最后一次失败摘要，并请求用户协助。

**绝对禁止**在未运行测试或未生成报告的情况下说出“完成”或“已修复”。

# 文件/文件夹忽略规则

## 在执行任务（包括搜索、读取、修改文件）时，**严格忽略**以下路径及其内容，不要尝试访问或分析它们：

- `.venv/`
- `.project/`